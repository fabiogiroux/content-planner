# CP-01.1 — Refatoração env vars multi-projeto

**Status:** Ready → InProgress  
**Epic:** CP-01

## O que fazer

Mover `PAGE_ID` e `IG_USER_ID` de hardcoded para env vars em `server.js`.

## Acceptance Criteria

- [ ] `PAGE_ID` lido de `process.env.IG_PAGE_ID`
- [ ] `IG_USER_ID` lido de `process.env.IG_USER_ID`
- [ ] App não sobe se variáveis obrigatórias não estão setadas (erro claro no stdout)
- [ ] Variáveis obrigatórias documentadas no README.md
- [ ] Instância girru existente continua funcionando (env vars preservadas)

## Variáveis de ambiente

```
IG_PAGE_ID=968835909655890          # girru (ou jnc)
IG_USER_ID=17841440286252925        # girru (ou jnc)  
INSTAGRAM_USER_TOKEN=<token>
APP_PASSWORD=<senha>                # novo — CP-01.2
PUBLIC_URL=https://...
PORT=3001
```

## Arquivos modificados

- [ ] `server.js` — trocar hardcoded por process.env
- [ ] `publish.js` — idem (se ainda em uso)
- [ ] `README.md` — criar/atualizar com env vars
